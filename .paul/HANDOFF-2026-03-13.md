# PAUL Handoff

**Date:** 2026-03-13
**Status:** paused

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** NORM — Notifier & Observer for Running Milestones
**Core value:** Developers managing multiple PALS projects can see the state of all their work in one place without switching terminals.

---

## Current State

**Version:** 0.1.0
**Phase:** 1 of 4 — Backend Core
**Plan:** 01-02 complete. 01-03 is next (not yet planned).

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop complete — ready for next PLAN]
```

---

## What Was Done

**This session:**
- Resumed from prior handoff (Plan 01-01 was already done)
- **Plan 01-02 fully executed and unified:**
  - Regex-based markdown parsers for STATE.md, ROADMAP.md, PROJECT.md (backend/parser.py)
  - Pydantic models for structured PALS state data (backend/parser_models.py)
  - SQLite cache tables: project_state, project_phases (backend/database.py)
  - CRUD functions for cache upsert/query (backend/models.py)
  - POST /api/projects/{id}/sync and GET /api/projects/{id}/state endpoints (backend/registry.py)
  - 21 unit + integration tests, all passing (tests/test_parser.py)
- PR #1 created, squash-merged to main (4fad924)
- Old feature/01-backend-core branch cleaned up
- Loosened .claude/settings.local.json — broad permissions with deny list for destructive ops
- Enabled experimental Claude Code features in ~/.zshrc:
  - CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
  - CLAUDE_CODE_EXPERIMENTAL_SUBAGENT_WORKTREES=1
  - CLAUDE_CODE_ENABLE_FINE_GRAINED_TOOL_STREAMING=1

---

## What's In Progress

- Nothing in progress — clean loop closure after Plan 01-02

---

## What's Next

**Immediate:** `/paul:plan` for Plan 01-03 — File watcher with change detection
- watchfiles integration for real-time .paul/ file monitoring
- Trigger sync when STATE.md/ROADMAP.md/PROJECT.md change
- This completes Phase 1 (Backend Core)

**After Phase 1:** Phase 2 — API & WebSocket (real-time push to frontend)

**Phase 1 roadmap (3 plans):**
- [x] 01-01: FastAPI scaffolding + SQLite + Registry
- [x] 01-02: Markdown parser for PALS state files
- [ ] 01-03: File watcher with change detection

---

## Key Decisions

| Decision | Rationale |
|----------|-----------|
| aiosqlite direct (no ORM) | Cache layer, not primary storage — simpler |
| Public repo | Free-tier GitHub needs public for branch protection rulesets |
| Conventional commits | feat:, fix:, chore: — trunk-based with PR flow |
| Phase 1 split into 3 plans | Scaffolding → Parser → Watcher |
| Regex parsing over markdown AST | No new deps, structured md has consistent formatting |
| Flat cache table + JSON columns | Simpler queries for blockers/decisions lists |
| Per-plan feature branches | Squash merge to main, clean branch per plan |

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview — 4 phases, 01-01 and 01-02 complete |
| `.paul/phases/01-backend-core/01-02-SUMMARY.md` | What was built in Plan 01-02 |
| `backend/main.py` | FastAPI app entry point |
| `backend/parser.py` | Markdown parsers for STATE/ROADMAP/PROJECT |
| `backend/parser_models.py` | Pydantic models for parsed state |
| `backend/registry.py` | Project registry + sync/state API |
| `backend/database.py` | SQLite schema (projects, project_state, project_phases) |
| `backend/models.py` | All CRUD + cache upsert functions |
| `tests/test_parser.py` | 21 parser tests |

---

## Tech Stack (Established)

| Layer | Technology |
|-------|------------|
| Backend | FastAPI + uvicorn |
| Database | SQLite via aiosqlite (direct, no ORM) |
| Config | pydantic-settings (NORM_ prefix) |
| Lint | ruff (line-length 100, py311) |
| Test | pytest + pytest-asyncio + httpx |
| CI | GitHub Actions (lint + test) |
| Frontend (future) | SvelteKit + shadcn-svelte + LayerChart |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume` or `/paul:progress`
3. Next action: `/paul:plan` for Plan 01-03 (File watcher)

---

*Handoff created: 2026-03-13*
