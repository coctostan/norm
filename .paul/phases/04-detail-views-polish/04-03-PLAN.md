---
phase: 04-detail-views-polish
plan: 03
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - backend/tests/test_parser.py
  - backend/tests/test_api.py
  - backend/tests/__init__.py
  - frontend/vitest.config.ts
  - frontend/src/lib/stores/websocket.svelte.test.ts
  - frontend/package.json
autonomous: false
---

<objective>
## Goal
Add backend API tests and frontend unit tests to validate core functionality before v0.1 release.

## Purpose
Phase 4 deferred all testing. This plan closes that gap with focused tests on the most critical paths: markdown parsing, API responses, and WebSocket store behavior.

## Output
- Backend pytest suite: parser tests + API endpoint tests
- Frontend vitest suite: WebSocket store unit tests
- All tests passing in CI-ready configuration
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Prior Work
@.paul/phases/04-detail-views-polish/04-02-SUMMARY.md

## Source Files
@backend/parser.py — markdown parser (STATE.md, ROADMAP.md, PROJECT.md)
@backend/registry.py — API routes (/api/projects, /api/projects/{id}/state)
@backend/schemas.py — Pydantic response models
@frontend/src/lib/stores/websocket.svelte.ts — WebSocket store with $state runes
@frontend/src/lib/types.ts — TypeScript interfaces
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /sveltekit-svelte5-tailwind | required | Before frontend test setup | ○ |

## Skill Invocation Checklist
- [ ] /sveltekit-svelte5-tailwind loaded
</skills>

<acceptance_criteria>

## AC-1: Backend parser tests pass
```gherkin
Given sample STATE.md, ROADMAP.md, and PROJECT.md content
When the parser functions are called
Then they return correctly structured data (phase, loop position, blockers, decisions, roadmap phases)
```

## AC-2: Backend API tests pass
```gherkin
Given a running test server with httpx AsyncClient
When GET /api/projects and GET /api/projects/{id}/state are called
Then they return valid JSON matching Pydantic schemas with correct status codes
```

## AC-3: Frontend store tests pass
```gherkin
Given vitest is configured for Svelte 5
When WebSocket store tests are run
Then connection state, message handling, and reconnect logic are validated
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Backend test suite — parser + API</name>
  <files>
    backend/tests/__init__.py,
    backend/tests/test_parser.py,
    backend/tests/test_api.py
  </files>
  <action>
    1. Create backend/tests/__init__.py (empty)

    2. In test_parser.py:
       - Test parse_state() with sample STATE.md content containing:
         phase info, loop position (plan ✓, apply ○, unify ○), blockers, decisions, progress
       - Test parse_roadmap() with sample ROADMAP.md content
       - Test parse_project() with sample PROJECT.md content
       - Test edge cases: empty content, missing sections

    3. In test_api.py:
       - Use httpx AsyncClient with FastAPI TestClient pattern
       - Test GET /api/projects returns 200 with list
       - Test GET /api/projects/{id}/state returns project state or 404
       - Test response models match expected schema structure

    4. Run: cd /Users/maxwellnewman/claude/norm && source .venv/bin/activate && pip install pytest pytest-asyncio httpx
  </action>
  <verify>cd /Users/maxwellnewman/claude/norm && source .venv/bin/activate && python -m pytest backend/tests/ -v</verify>
  <done>AC-1, AC-2 satisfied: Backend parser and API tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Frontend vitest setup + WebSocket store tests</name>
  <files>
    frontend/vitest.config.ts,
    frontend/src/lib/stores/websocket.svelte.test.ts,
    frontend/package.json
  </files>
  <action>
    1. Install vitest and test deps:
       cd /Users/maxwellnewman/claude/norm/frontend && npm install -D vitest @testing-library/svelte jsdom

    2. Create vitest.config.ts:
       - Configure svelte plugin for Svelte 5 runes
       - Set test environment to jsdom
       - Set include pattern for *.test.ts files

    3. In websocket.svelte.test.ts:
       - Mock WebSocket (global mock class)
       - Test: connect() creates WebSocket with correct URL
       - Test: disconnect() closes connection and sets connected=false
       - Test: onmessage with initial_state updates projects array
       - Test: onmessage with project_updated updates projects array
       - Test: reconnect logic with exponential backoff (onclose triggers setTimeout)

    4. Add test script to package.json: "test": "vitest run", "test:watch": "vitest"
  </action>
  <verify>cd /Users/maxwellnewman/claude/norm/frontend && npm run test</verify>
  <done>AC-3 satisfied: Frontend store tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Backend pytest suite + frontend vitest suite covering parser, API, and WebSocket store</what-built>
  <how-to-verify>
    1. Run backend tests: cd /Users/maxwellnewman/claude/norm && source .venv/bin/activate && python -m pytest backend/tests/ -v
    2. Run frontend tests: cd frontend && npm run test
    3. Confirm all tests pass with no failures
    4. Run full build: cd frontend && npm run check && npm run build
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- backend/parser.py (test it, don't modify it)
- backend/registry.py (test it, don't modify it)
- backend/schemas.py (test it, don't modify it)
- frontend/src/lib/stores/websocket.svelte.ts (test it, don't modify it)
- frontend/src/routes/ (no page changes in this plan)
- frontend/src/lib/components/ (no component changes)

## SCOPE LIMITS
- No e2e browser tests (Playwright) — deferred beyond v0.1
- No visual regression tests — manual verification sufficient for v0.1
- No performance benchmarks — basic functionality testing only
- Focus on unit and integration tests only

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `cd /Users/maxwellnewman/claude/norm && source .venv/bin/activate && python -m pytest backend/tests/ -v` — all pass
- [ ] `cd frontend && npm run test` — all pass
- [ ] `cd frontend && npm run check` — zero TypeScript errors
- [ ] `cd frontend && npm run build` — production build succeeds
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All backend tests pass
- All frontend tests pass
- No type errors introduced
- Production build still succeeds
- Tests are CI-ready (no manual setup required)
</success_criteria>

<output>
After completion, create `.paul/phases/04-detail-views-polish/04-03-SUMMARY.md`
</output>
