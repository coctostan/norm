---
phase: 04-detail-views-polish
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - frontend/src/app.css
  - frontend/src/routes/+layout.svelte
  - frontend/src/routes/+page.svelte
  - frontend/src/routes/project/[id]/+page.svelte
  - frontend/src/lib/components/project-card.svelte
  - frontend/svelte.config.js
autonomous: false
---

<objective>
## Goal
Polish the dark mode UI for Grafana-level density and Linear-level refinement, and configure a production adapter.

## Purpose
The current UI is functional but uses default shadcn-svelte zinc theme styling. Polish brings it closer to the design inspirations (Grafana density, Linear polish, Vercel dark) and production adapter makes the app deployable.

## Output
- Refined dark theme with tighter spacing, subtle borders, and improved typography hierarchy
- Dashboard header with project count and last-updated timestamp
- Detail page visual polish (section spacing, roadmap timeline feel)
- Production adapter configured (adapter-node for local deployment)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Prior Work
@.paul/phases/04-detail-views-polish/04-01-SUMMARY.md

## Source Files
@frontend/src/app.css — theme variables (zinc OKLCH)
@frontend/src/app.html — dark class on html
@frontend/src/routes/+layout.svelte — app shell with header
@frontend/src/routes/+page.svelte — dashboard page
@frontend/src/routes/project/[id]/+page.svelte — detail page
@frontend/src/lib/components/project-card.svelte — project card
@frontend/svelte.config.js — adapter config
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /sveltekit-svelte5-tailwind | required | Before styling and adapter work | ○ |
| /shadcn-svelte | required | Before component styling | ○ |

## Skill Invocation Checklist
- [ ] /sveltekit-svelte5-tailwind loaded
- [ ] /shadcn-svelte loaded
</skills>

<acceptance_criteria>

## AC-1: Dashboard layout has Grafana-like density
```gherkin
Given the dashboard loads with project cards
When viewed on desktop (>1024px)
Then cards have tighter padding, smaller text, and more information density
And the header shows project count and connection status on one line
```

## AC-2: Detail page has polished section layout
```gherkin
Given a user navigates to /project/{id}
When the detail page loads
Then sections have consistent spacing, subtle dividers, and clear visual hierarchy
And the roadmap has a timeline-like appearance with connected phase indicators
```

## AC-3: Production adapter configured
```gherkin
Given the frontend project
When npm run build is executed
Then it builds successfully with adapter-node
And outputs a production-ready Node.js server
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Polish dashboard and cards for density</name>
  <files>
    frontend/src/routes/+layout.svelte,
    frontend/src/routes/+page.svelte,
    frontend/src/lib/components/project-card.svelte
  </files>
  <action>
    1. In +layout.svelte:
       - Tighten header: reduce py-4 to py-3, make subtitle smaller
       - Add subtle bottom border glow or gradient for depth

    2. In +page.svelte:
       - Add project count next to "Dashboard" heading: "Dashboard (N projects)"
       - Tighten spacing: space-y-6 to space-y-4
       - Make blocker banner more compact (py-3 to py-2)

    3. In project-card.svelte:
       - Reduce card padding (Card.Header pb-3 to pb-2, tighter Content spacing)
       - Make loop badges smaller (text-xs already, reduce gap)
       - Tighter progress bar section
       - Add subtle hover elevation (shadow-sm to shadow-md on hover)

    Design reference: Grafana panel density — more data in less space without feeling cramped.
  </action>
  <verify>npm run check && npm run build</verify>
  <done>AC-1 satisfied: Dashboard has tighter, denser layout</done>
</task>

<task type="auto">
  <name>Task 2: Polish detail page and configure adapter-node</name>
  <files>
    frontend/src/routes/project/[id]/+page.svelte,
    frontend/svelte.config.js
  </files>
  <action>
    1. In +page.svelte detail page:
       - Add vertical connecting line between roadmap phase items (left border or pseudo-element via Tailwind)
       - Tighten section spacing for density
       - Style the status bar card with subtle gradient or accent border-left
       - Make decisions table more compact (smaller text, tighter rows)
       - Add "Back to Dashboard" as a proper breadcrumb-style nav

    2. In svelte.config.js:
       - Replace adapter-auto with adapter-node
       - Install: npm install -D @sveltejs/adapter-node
       - Configure output directory
  </action>
  <verify>
    - npm run check passes
    - npm run build succeeds with adapter-node (outputs build/ directory)
  </verify>
  <done>AC-2, AC-3 satisfied: Detail page polished, production build works</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dark mode polish across dashboard and detail pages, production adapter</what-built>
  <how-to-verify>
    1. Start backend and frontend dev servers
    2. Visit http://localhost:5173 — check dashboard density (tighter cards, project count)
    3. Click into a project — check detail page polish (roadmap timeline, section spacing)
    4. Compare visual density to Grafana/Linear reference style
    5. Run: cd frontend && npm run build — confirm adapter-node build succeeds
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- backend/ (no backend changes in this plan)
- frontend/src/lib/stores/websocket.svelte.ts (store is stable)
- frontend/src/lib/types.ts (types are stable)
- frontend/src/routes/project/[id]/+page.ts (load function is stable)

## SCOPE LIMITS
- No new shadcn-svelte components — use existing Card, Badge, Progress, Separator
- No LayerChart yet — that's beyond v0.1 scope
- No functional changes — this is purely visual polish + adapter config
- No color theme changes — keep zinc dark theme, just adjust spacing/density

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run check` — zero TypeScript errors
- [ ] `cd frontend && npm run build` — production build succeeds with adapter-node
- [ ] Dashboard visually denser without feeling cramped
- [ ] Detail page sections have clear hierarchy and spacing
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Visual polish matches Grafana density / Linear refinement reference
- Production build outputs Node.js server
</success_criteria>

<output>
After completion, create `.paul/phases/04-detail-views-polish/04-02-SUMMARY.md`
</output>
